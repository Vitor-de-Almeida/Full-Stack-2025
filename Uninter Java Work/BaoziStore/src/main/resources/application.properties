spring.application.name=BaoziStore

# MySQL: application-mysql.properties + application-local.properties (senha local, fora do Git).
# Sem MySQL: --spring.profiles.active=h2
spring.profiles.active=mysql,local

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
